this is just a very simple browser (realistically a gui connecting to a server that fetches url inputs and formats) with the backend written in Go and 2 frontend GUIs. basically tkinter was the first gui but tkinter when visiting certain sites freezes and crashes,
so i made a gui in C# (this is my first time using C# so i used an LLM for help writing the logic) and it handles URLs much better but you have to type all the urls manually as link tags dont redirect to their ref'd links for some reason... so, pros and cons to both.

to run:

1. have go and python/C# installed + vscode and visual studio

2. run the go server with go run main.go

3. depending on the gui if your choice, run the C# gui in visual studio or if python, do python frontendtkinter.py

4. enter a url

5. ????

6. profit
